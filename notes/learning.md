# Key Learnings

* Default Windows logging is insufficient for detecting attacks
* Sysmon significantly improves visibility into system activity
* Port scanning may not always generate logs unless configured
* Reverse shells create detectable outbound traffic patterns
* Detection requires understanding both attacker behavior and log sources
